language: go

sudo: false

go:
  - 1.6

before_install:
  - git submodule update --init --recursive

before_script:
  - make fmt
  - make vet

script:
  - make test

after_success:
  - bash <(curl -s https://codecov.io/bash)