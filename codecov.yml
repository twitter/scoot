coverage:
  status:
    project:
      default:  # status context
        target: auto
        threshold: null
    patch:
      default:
        target: auto
        threshold: null
    changes:
      default:
        target: auto