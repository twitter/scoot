/*
Package client provides a CLI implementation of a client for the
Cloud Scoot API. The current implementation is tethered to the
CLI processing, and the client interface is not independent.
The CLI client resolves the Cloud Scoot Server via a Dialer
(common/dialer).
*/
package cli
